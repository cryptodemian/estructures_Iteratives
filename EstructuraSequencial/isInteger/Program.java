/*
* Program.java        1.0 09/26/2025
*  
* Verify if number is integer or not.
* 
* Copyright 2025 Demian Melnyk 
*/

public class Program {

	/**
	 * Verify if number is integer or not.
	 * 
	 * @param start_int an integer to be added.
	 * @param end_int an integer number to be added.
	 * @return the number is between start_int & end_int
	 */
    public boolean isInteger(int number) {
		boolean isInteger = ;
		return isInteger;
    }
    
    /**
     * 
     * @param args not used.
     */
    public static void main(String[] args) {
		int isInteger;
		boolean isInOpenInterval;
		//Print the title of the program
		System.out.print("\nVERIFY IF NUMBER IS INSIDE THE INTERVAL OR NOT. DEMIAN\n\n");
		start_int = 3.1;
		//PRocess the data
		Program p = new Program();
		isInteger = p.isInteger(number);
		System.out.print(isInteger);
	}
}

